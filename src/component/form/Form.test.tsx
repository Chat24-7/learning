import React from 'react'
import { render, screen } from "@testing-library/react"
import '@testing-library/jest-dom'

describe("Form priority cases", () => {

    test("user should be able to submit form and recieve status", () => {
        //throw new Error("err")
    })

    test("user should be able to fill the form feilds", () => {
        //throw new Error("err")
    })

})

describe("Form validation", () => {

    test("user should not be able to submit if fiels are empty", () => {
        //throw new Error("err")
    })

})

describe("Form basic test", () => {

    test("form should render properly", () => {
        //throw new Error("err")
    })

    test("form should display header/title as per passed props", () => {
        throw new Error("err")
    })


})